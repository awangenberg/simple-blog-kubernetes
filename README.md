# simple-blog-kubernetes

### API

To be able to run the api locally you'll need a .env file in the api root which contains the following values:

```
MYSQL_ROOT_PASSWORD=password
RUNENVIRONMENT=Development

```